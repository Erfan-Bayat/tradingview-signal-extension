{
  "name": "tradingview-signal-extension",
  "version": "0.1.0",
  "private": true,
  "description": "TradingView Signal Executor & Monitor Chrome Extension",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
